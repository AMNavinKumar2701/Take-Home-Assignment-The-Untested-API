{
  "name": "task-api",
  "version": "1.0.0",
  "description": "Task Manager API",
  "main": "src/app.js",
  "scripts": {
    "start": "node src/app.js",
    "test": "jest",
    "coverage": "jest --coverage",
    "seed": "node scripts/seedTasks.js",
    "benchmark:read": "autocannon -c 100 -d 5 http://localhost:3000/tasks",
    "benchmark:write": "node scripts/benchmarkWrite.js"
  },
  "dependencies": {
    "express": "^4.18.2",
    "uuid": "^9.0.0"
  },
  "devDependencies": {
    "autocannon": "^8.0.0",
    "jest": "^29.7.0",
    "supertest": "^6.3.4"
  }
}
